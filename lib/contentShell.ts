/** Viewport horizontal padding — “outer box” (nav CL + links, mail icon, ©). No max-width. */
export const pageEdgeClass = "w-full px-4 md:px-[100px]";

/** Inner column: same padding + centered max width for hero copy, name band, link row. */
export const contentShellClass =
  "mx-auto w-full max-w-[1500px] px-4 md:px-[100px]";

/** Standard vertical spacing for main page sections below the hero. */
export const sectionPaddingClass = "py-10 md:py-20 lg:py-28";
